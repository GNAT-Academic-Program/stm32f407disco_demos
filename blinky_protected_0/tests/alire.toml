name = "blinky_protected_0_tests"
description = "Tests for the built-in Alire test runner"
version = "0.0.0-test"

[[depends-on]]
blinky_protected_0 = "*"

[[pins]]
blinky_protected_0 = { path = ".." }

[build-profiles]
blinky_protected_0       = "validation"
blinky_protected_0_tests = "validation"
