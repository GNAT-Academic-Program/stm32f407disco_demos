name = "blinky_tests"
description = "Tests for the built-in Alire test runner"
version = "0.0.0-test"

[[depends-on]]
blinky = "*"

[[pins]]
blinky = { path = ".." }

[build-profiles]
blinky       = "validation"
blinky_tests = "validation"
